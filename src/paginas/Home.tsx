import { Button } from "react-bootstrap";

export default function Home() {
  return (
    <div className="p-3">
      <h1>Bem-vindo ao MeuApp</h1>
      <p>Este é o template base usando React + React Bootstrap.</p>
      <Button variant="primary">Saiba mais</Button>
    </div>
  );
}
